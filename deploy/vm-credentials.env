# ============================================================
# ARIS 4.0 — VM Connection Parameters
# AU-IBAR Infrastructure — Nairobi Data Center
# Last updated: 2026-03-09
# ============================================================
# CONFIDENTIAL — Do not commit to public repositories
# ============================================================

# Common credentials
VM_USER=arisadmin
VM_PASSWORD='@u-1baR.0rg$U24'

# VM-APP — Application Server (22 microservices + Frontend)
VM_APP_HOSTNAME=nbo-aris04
VM_APP_IP=10.202.101.183
VM_APP_VCPU=16
VM_APP_RAM=32GB
VM_APP_DISK=300GB_SSD
VM_APP_ROLE="22 microservices Fastify + Next.js 14 + Traefik API Gateway"

# VM-KAFKA — Message Bus (Kafka KRaft)
VM_KAFKA_HOSTNAME=nbo-brk01
VM_KAFKA_IP=10.202.101.184
VM_KAFKA_VCPU=8
VM_KAFKA_RAM=16GB
VM_KAFKA_DISK=500GB_NVMe
VM_KAFKA_ROLE="3 Kafka Brokers KRaft + Schema Registry + Kafka UI"

# VM-DB — Database (PostgreSQL 16 + PostGIS + PgBouncer)
VM_DB_HOSTNAME=nbo-dbms03
VM_DB_IP=10.202.101.185
VM_DB_VCPU=8
VM_DB_RAM=32GB
VM_DB_DISK=1TB_SSD_HDD
VM_DB_ROLE="PostgreSQL 16 + PostGIS 3.4 + PgBouncer"
VM_DB_LUKS_PASSPHRASE='@u-1baR.0rg$U24'
VM_DB_NOTE="LUKS passphrase = arisadmin password (as configured by user)"

# VM-CACHE — Cache & Search (Redis 7 + OpenSearch)
VM_CACHE_HOSTNAME=nbo-cch01
VM_CACHE_IP=10.202.101.186
VM_CACHE_VCPU=8
VM_CACHE_RAM=32GB
VM_CACHE_DISK=200GB_SSD
VM_CACHE_ROLE="Redis 7 + OpenSearch"

# Network
VLAN_SUBNET=10.202.101.0/24
GATEWAY=10.202.101.1
DNS_1=10.202.101.2
DNS_2=10.202.101.3

# SSH Configuration
SSH_PORT=22
SSH_AUTH_METHODS="publickey,password"
SSH_BANNER="SSH-2.0-OpenSSH_9.6p1 Ubuntu-3ubuntu13.14"
OS_VERSION="Ubuntu Server 24.04 LTS (Noble Numbat)"
