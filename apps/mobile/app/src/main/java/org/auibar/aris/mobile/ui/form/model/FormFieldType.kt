package org.auibar.aris.mobile.ui.form.model

enum class FormFieldType {
    TEXT,
    NUMBER,
    DATE,
    SELECT,
    MULTI_SELECT,
    LOCATION,
    SPECIES_SELECTOR,
    DISEASE_SELECTOR,
    ADMIN_CASCADER,
    PHOTO_CAPTURE,
}
