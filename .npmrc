# Hoist all dependencies to root node_modules for workspace-wide accessibility.
# Without this, services that import workspace packages without declaring them
# in package.json will fail to resolve (pnpm strict mode).
shamefully-hoist=true
