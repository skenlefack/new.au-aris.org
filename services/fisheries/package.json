{
  "name": "@aris/fisheries-service",
  "version": "0.1.0",
  "private": true,
  "description": "ARIS 4.0 — Fisheries & Aquaculture service: captures, fleet, aquaculture, aquatic health",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "db:seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@aris/shared-types": "workspace:*",
    "@aris/kafka-client": "workspace:*",
    "@aris/auth-middleware": "workspace:*",
    "@prisma/client": "^6.2.0",
    "@sinclair/typebox": "^0.32.0",
    "fastify": "^5.2.0",
    "@fastify/cors": "^10.0.0",
    "dotenv": "^17.3.1",
    "ioredis": "^5.3.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "prisma": "^6.2.0",
    "tsx": "^4.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
